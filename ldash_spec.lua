describe('Ternary / Null Coalesce', function()
  test('Ternary', function()
    assert.are.equal(1, 1)
  end)

  test('Null Coalesce', function()
    assert.are.equal(1, 1)
  end)
end)

describe('Iterators', function()
  test('pairs', function()
    assert.are.equal(1, 1)
  end)

  test('kpairs', function()
    assert.are.equal(1, 1)
  end)

  test('ipairs', function()
    assert.are.equal(1, 1)
  end)
end)
